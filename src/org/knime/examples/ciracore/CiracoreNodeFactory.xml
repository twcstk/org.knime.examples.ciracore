<?xml version="1.0" encoding="UTF-8"?>
<knimeNode icon="./ciracore16.png" type="Source" xmlns="http://knime.org/node/v2.12" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://knime.org/node/v2.12 http://knime.org/node/v2.12.xsd">
    <name>Ciracore Connector</name>
    
    <shortDescription>
     Connects to MongoDB in order to access it by the downstream nodes.   
    </shortDescription>
    
    <fullDescription>
<intro>
            Connects to MongoDB in order to access it by the downstream nodes.
        </intro>
        <tab name="Settings">
        	<option name="Connection type">
        		Use standard for a <a href="https://docs.mongodb.com/manual/reference/connection-string/#std-label-connections-standard-connection-string-format">standard</a> 
        		and SRV for a <a href="https://docs.mongodb.com/manual/reference/connection-string/#std-label-connections-dns-seedlist">DNS Seed List</a> connection.</option>
	        <option name="Instances">
	        	List of servers to connect to. The default port for standard connections is 27017.
	        	For DNS seed list (SRV) connections the port must be empty.
        	</option>
	        <option name="Authentication">
	        	When you select Credential or Username and password you also have to specify the authentication database.
	        	Select the None option to use no authentication at all or an alternative authentication.
	        	To use an alternative authentication you have to specifying the required parameters via the Advanced tab.
	        	For more details see the <a href="https://docs.mongodb.com/manual/core/authentication/">MongoDB manual</a>.
        	</option>
	        <option name="Authentication Database">
	        	The <a href="https://docs.mongodb.com/manual/core/security-users/#authentication-database">authentication database</a> 
	        	for the login user. This option is only enabled for the Credential or Username and password authentication type. 
	        	</option>
        </tab>
        <tab name="Advanced">
        	<option name="Connection string options ">
        		The table provides a list of standard options and their default value if available.
	        	For further details about the available connection string options please have a look at the 
	        	<a href="https://docs.mongodb.com/manual/reference/connection-string/#connection-string-options">Mongo DB documentation</a>.
        	</option>
        </tab>

    </fullDescription>
    
    <ports>
        <outPort index="0" name="MongoDB Connection">The MongoDB Connection</outPort>
    </ports>
 
</knimeNode>
